# Screenshots

The README embeds the images in this folder. Drop your screenshots here using these exact
file names so the links resolve:

| File | Screen to capture |
| ---- | ----------------- |
| `dashboard.jpg` | The Dashboard view with its metric cards |
| `compiler.jpg` | The Policy Compiler with a compiled rule on the right |
| `claims.jpg` | The Claims Adjudication table after running adjudication |
| `review-queue.jpg` | The Review Queue with the two routed claims |
| `policy-diff.jpg` | The Policy Change Diff with a before and after |

Tips for clean captures:

- Use a wide browser window so the full layout is visible.
- PNG keeps text crisp. Aim for roughly 1600 pixels wide.
- A light background reads best against the README.
